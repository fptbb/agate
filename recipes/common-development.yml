---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: script
    scripts:
      - nix.sh
  - type: justfiles
    validate: true
    include: # use just --fmt --check --unstable --justfile to validate justfiles
      - development.just
      - fido.just
      - gpg.just
