---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: fonts
    fonts:
      nerd-fonts:
        - NerdFontsSymbolsOnly
  - type: script
    scripts:
      - branding.sh
      - os-birthday.sh

  - type: systemd
    system:
      enabled:
        - os-birthday
